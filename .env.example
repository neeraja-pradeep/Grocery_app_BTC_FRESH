# Template for the runtime environment file.
# Copy this to `.env` and fill in real values:  cp .env.example .env
# The real `.env` is git-ignored so secrets never get committed.

# Main backend API server (no trailing slash).
API_BASE_URL=http://156.67.104.149:8080

# Razorpay key id. Use rzp_test_* for staging, rzp_live_* for production.
RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxxxx
